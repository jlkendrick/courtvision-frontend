package main

import (
	"fmt"
)

// Genetic algorithm to optimize streaming moves for the week
func optimize_streaming(free_agent_map map[string]Player, free_positions map[int]map[string]bool, week string) {

	fmt.Println(free_positions)

}